# NERIS frontend export settings (optional fallback)
# Prefer Admin Functions -> Customization for these values.
# Do not commit real secrets in .env files.

VITE_NERIS_EXPORT_URL=/api/neris/export
VITE_NERIS_VENDOR_CODE=
# Optional: only used when calling non-proxy endpoints directly.
VITE_NERIS_SECRET_KEY=
# Optional defaults:
# VITE_NERIS_VENDOR_HEADER_NAME=X-NERIS-Entity-ID
# VITE_NERIS_AUTH_HEADER_NAME=Authorization
# VITE_NERIS_AUTH_SCHEME=Bearer
# VITE_NERIS_CONTENT_TYPE=application/json
