# Session note — 2026-04-09 (agent handoff)

**Branch:** `submenu/neris-golive-cifpd`

## CAD plan docs — single source of truth

- **`docs/plans/CAD_DISPATCH_PARSING_IMPLEMENTATION_PLAN.md`** now holds **Part 1 (product/UX)** + **Part 2 (phases + batches)**.
- **Removed:** `CAD_EMAIL_PARSING_AND_INCIDENT_AUTOCREATE_PLAN.md` (content merged; indexes updated: `EMAIL_AND_CAD_SETUP.md`, `docs/procedures/README.md`, `docs/PRIORITY_WHAT_NEEDS_TO_BE_COMPLETED.md`, `ACTIVE_CONTEXT.md`).

## Batch E — CAD dispatch rule engine

- **`src/cadDispatch/`:** `normalizeDispatchText.ts`, `ruleEngine.ts` (`parseCadRulesJson`, `runCadRulePipeline`, `CadRule` union), `icommFixtures.ts`, `index.ts` barrel.
- **Tests:** `npm run test` → `node --import tsx/esm --test src/cadDispatch/ruleEngine.test.ts`. **`tsconfig.app.json`** excludes `**/*.test.ts` from app `tsc`.
- **Client:** `src/api/cadDispatchConfig.ts` — `messageRules` / `incidentRules` typed as `CadRule[]`.
- **Migrations:** User confirmed **`20260409140000_add_cad_parsing_settings_and_allowlist`** applied staging/prod in a prior chat — treat as done unless user says otherwise.
- **Not done yet:** `server/neris-proxy.mjs` does **not** import TS rule engine — **Batch G** needs a bridge (prebuild, `tsx`, or small wrapper).

## Batch F — Incident Parsing UI

- **`src/pages/DispatchParsingIncidentPanel.tsx`:** Load/save `enableIncidentCreation` + `incidentRules` via `getCadParsingConfig` / `patchCadParsingConfig`; JSON editor + **Preview** (`parseCadRulesJson` + `runCadRulePipeline`); sample text, ICOMM sample, load plain text from latest `CadEmailIngest` row.
- **`src/cadDispatch/extractDispatchPlainText.ts`:** Shared MIME/base64 → plain text (Raw Email panel refactored to use it).
- **Next:** Batch **G** — server ingest calls rule engine + creates/updates incidents.

## Handoff folder convention (corrected this session)

- **`sessions/`:** **one file per working session** — agent-oriented: what shipped, paths, verification, next steps. Do **not** add a new session file per sub-task in the same session.
- **`conversations/`:** **one `YYYY-MM-DD-session-summary.md` per working session** — user-oriented condensed summary of the conversation. Same rule: not multiple micro-summaries per day for separate topics.

## Verified

- `npm run lint`, `npm run test`, `npm run build` — pass (after Batch E + F).
